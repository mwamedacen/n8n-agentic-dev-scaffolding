# airtable integration-skills

Agent-authored. Add a markdown file when you learn something non-obvious about how the **airtable** integration works in n8n — node types, credential gotchas, parameter shapes, response quirks, rate limits, etc.

See `SKILL.md` and the existing folders (`microsoft-365/`, `gmail/`, `redis/`) for shape.
